version: '3.8'
services:
  ai-market-analyst:
    build: .
    ports:
      - "8000:8000"
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./cache:/app/cache
      - ./uploads:/app/data/uploads
    restart: unless-stopped